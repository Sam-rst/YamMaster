name: CI/CD Preview

on:
  push:
    branches: [preview]

jobs:
  # TODO: réactiver ci + deploy-backend après validation du fix
  # ci:
  #   name: Lint → Tests → Build
  #   uses: ./.github/workflows/_ci-shared.yml
  #   secrets: inherit

  # deploy-backend:
  #   name: Déploiement Backend Preview
  #   runs-on: ubuntu-latest
  #   needs: ci
  #   environment: preview
  #   steps:
  #     - name: Trigger Render Deploy
  #       run: |
  #         echo "🧪 Déploiement backend en environnement PREVIEW"
  #         if [ -n "${{ secrets.RENDER_DEPLOY_HOOK }}" ]; then
  #           curl -s "${{ secrets.RENDER_DEPLOY_HOOK }}"
  #           echo "Deploy hook triggered"
  #         else
  #           echo "⚠️ RENDER_DEPLOY_HOOK non configuré"
  #           exit 1
  #         fi

  deploy-frontend:
    name: Déploiement Frontend Preview
    runs-on: ubuntu-latest
    environment: preview
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Pull Vercel Environment
        run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}

      - name: Export Vercel env vars
        run: |
          grep -v '^#' .vercel/.env.preview.local | grep '=' >> $GITHUB_ENV

      - name: Build Frontend
        run: vercel build --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Preview
        run: vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }}
