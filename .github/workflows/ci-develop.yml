name: CI Develop

on:
  push:
    branches: [develop]

jobs:
  ci:
    name: Lint → Tests → Build
    uses: ./.github/workflows/_ci-shared.yml
    secrets: inherit
    # Pas de déploiement cloud depuis develop — développement local uniquement
